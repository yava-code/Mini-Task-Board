# Mini-Task-Board

Делали вдвоём. Простенький клон Trello для практики парной работы и фронтенда (колонки, карточки, API на Flask).

```bash
pip install -r requirements.txt
python app.py
```

Открой `index.html` в браузере (или через Live Server) — фронт ходит на `http://localhost:5000`.
